# Config

Version-controlled trust, monitor targets, and dependency declarations.

Phase 0 is empty on purpose. Do not add fabricated protocol edges or trusted wallets to look complete.

- `trust/` — publisher registry (Phase 1/3)
- `monitors/` — observation targets (Phase 5/6)
- `dependencies/` — evidence-backed edges (Phase 7)
